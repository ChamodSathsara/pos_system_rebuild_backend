# Helpers

Currently empty. Claims/principal helpers live in `Extensions/ClaimsPrincipalExtensions.cs`
since they're implemented as extension methods. Add non-extension-method helper classes
(formatters, code generators, mapping utilities that don't belong in AutoMapper profiles, etc.)
here as the project grows.
